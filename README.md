# ml-Rock-Paper-Scissor
A machine learning project that uses a Convolutional Neural Network (CNN) to classify hand gestures for Rock, Paper, and Scissors. The model is trained on image data to recognize and predict the correct gesture.
